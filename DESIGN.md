# Sim3dView 設計書

## 0. 本書の位置づけ

[claude_code_instructions.md](claude_code_instructions.md)(以下「指示書」)を土台とし、`map_data/` に実際に配置されたALOS全球数値地表モデル(AW3D30相当, PRISM/AVNIR-2由来)データの実態を調査したうえで、指示書内「未確定・実装時に判断/確認が必要な項目」全12項目のうちユーザーとの対話で確定させた版である。

| # | 項目 | 確定内容 |
|---|---|---|
| 1 | 再接続処理 | **自動リトライする**(指数バックオフ、無制限リトライ。4.5節) |
| 2 | マルチクライアント | **全員同一ブロードキャストでよい** |
| 3 | VABの行数・列数 | 開発用ダミー`VabConfig`の初期値は**縦4行×横6列**(6.2節) |
| 4 | VABの操作種別 | **単純クリックのみでよい**(6.2節) |
| 5 | 状況パネル上部「各種情報」の表示項目 | **C++側(サーバー)から動的に設定できる**ようにする。新規`StatusPanelConfig`メッセージを追加(4.4節、6.3節) |
| 6 | 左右パネルのレスポンシブ対応 | 左パネルは固定でレスポンシブ不要。**地図(中央)と右パネルはレスポンシブ対応させ、かつユーザーがUIでサイズを変更できる**ようにする(6.1節) |
| 7 | 地形エリアの規模 | `map_data`内17タイル**全体をモザイク**し、単一の広域メッシュとする |
| 8 | オルソ画像の有無 | RGBオルソ画像なし。パンクロ画像(STK.tif)も**使用せず、標高グラデーション着色のみ**とする |
| 9 | メッシュ解像度 | **1024×1024**(約105万頂点) |
| 10 | 垂直誇張(vertical exaggeration)の要否 | **不要**。実メートル値のまま描画する(誇張機能は実装しない) |
| 11 | 座標系のすり合わせ | 基準位置(原点)は**UIから緯度経度を入力**して指定する。原点=入力地点の海抜0m地点。**東=X、北=Y、鉛直上向き=Z**の局所ENU座標系をシミュレーション座標系とする。**シミュレーション本体(C++側)もUIと全く同じ座標系・原点を用いる**(内部的に別の座標表現は持たない、3節・4節参照)。デフォルト(試験用)原点は**北緯35°21'20"、東経138°51'35"**(=35.355556°, 138.859722°) |
| 12 | カメラの自由視点化 | 固定カメラのMVPは採用せず、**ズーム(拡大縮小)・角度切り替え(回転)・視点切り替えを自由に操作できる**カメラとする(5.2節) |

指示書に書かれた内容(通信プロトコル、C++/Rustのディレクトリ構成、VAB仕様等)は基本的にそのまま踏襲する。本書は指示書の内容を実データ・上記12項目の確定事項に合わせて具体化するものであり、指示書と矛盾する記載がある箇所は本書を優先する。

原点がUI入力によるランタイムパラメータになったことで、地形前処理(オフラインGDALバッチ)を原点ごとに再実行するのは非現実的(原点変更のたびにGDAL処理を待つのはUXとして不適切)。そのため、**前処理は原点非依存の生の緯度経度グリッドとして地形データを出力し、原点に対する座標変換(ENU変換)はフロント側(Rust/WASM)がランタイムに行う**設計とする。これに伴い前処理側の座標系設計を全面的に見直した(2.2節)。

---

## 1. 対象地形データの実態調査結果

### 1.1 ファイル構成(1タイルあたり)

`map_data/ALPSMLC30_<TILEID>_*` の形式で、17タイル分×6ファイル = 102ファイル。

| サフィックス | 内容 | 本設計での用途 |
|---|---|---|
| `_DSM.tif` | 数値表層モデル(標高、GeoTIFF, 3600×3600px) | **使用**(唯一の入力ラスタ) |
| `_MSK.tif` | 品質マスク(雲・水域等のフラグ) | 今回は不使用(v1では無視。欠損対応で将来使う余地あり) |
| `_STK.tif` | パンクロマチック(白黒)画像 | **不使用**(ユーザー確定事項) |
| `_HDR.txt` | タイルのヘッダ情報(四隅座標・解像度・楕円体等) | 前処理ツールのテスト・検証用参考情報 |
| `_LST.txt` | 元シーン(観測パス)のリスト | 使用しない |
| `_QAI.txt` | 品質指標(SRTM/ASTERとの差分統計等) | 使用しない |

### 1.2 タイル分布と規模

- タイルID命名: `N<緯度2桁>E<経度3桁>` = タイル南西角の整数度。1タイル = 経緯度1°×1°。
- 実在タイル(17枚、対角状に分布。緯度が上がるほど東経範囲が狭まる):

```
lat39:                                              E139
lat38:                                      E138 E139
lat37:                            E136 E137 E138 E139
lat36:      E135 E136 E137 E138 E139
lat35:      E135 E136 E137 E138 E139
```

- 外接矩形: **北緯35°〜40°、東経135°〜140°**(5°×5°の格子のうち17/25セルが実在。北緯37°で東経135°、北緯38°で東経135〜137°、北緯39°で東経135〜138°が**欠損**)
- 実距離換算で概ね**南北550km×東西450km**規模(この緯度帯では経度1°≈90km、緯度1°≈111km)
- 解像度: 1秒角(3600px/度)= 南北方向約30m/px、東経136〜139°付近(緯度35〜39°)では東西方向は緯度のcos分だけ狭く約24〜27m/px

### 1.3 標高データの実態

全17タイルを実際にスキャンした結果:

- 標高範囲: **-102.0m 〜 3771.0m**(最大値は東経138°/北緯35°タイル内、位置的に富士山と整合)
- 明示的なnodataセンチネル値(-9999等)は**検出されなかった**。負値は沿岸部・湖沼付近の実測値と推測される
- データ型: GeoTIFF、符号付き整数(16bit相当。前処理でf32メートルに変換)

### 1.4 欠損タイル(外接矩形に対して存在しない8セル)への対応方針

外接矩形(5°×5°)でモザイクすると、実在しない8タイル分は無データ領域になる。方針:

- モザイク時、無データ領域は **標高0m(海面相当)で一律埋める**(v1のシンプルな方針。実際には隣接陸地と地続きの場合もあるが、対象外領域なので許容する)
- `metadata.json` に `filled_nodata: true` 相当のフラグは設けず、まずは前処理ツール内のコメントとして方針を明記するに留める(要件が変われば拡張)

---

## 2. GeoTIFF前処理ツール仕様(確定版)

指示書 8.節の処理ステップを、実データに合わせて具体化する。

### 2.1 入力

- `map_data/*_DSM.tif` 全17枚固定(ツール起動時にglobで収集。将来タイル追加にも対応できるようディレクトリ内`_DSM.tif`を機械的に列挙する)

### 2.2 座標系(再投影は不要という結論)

ALOS DSMタイルは元々**EPSG:4326(WGS84/GRS80楕円体)の緯度経度グリッド**で、1タイル=1°×1°=3600×3600pxちょうどに整列している(タイル境界での位置ズレなし)。原点(=座標変換の基準)がUI入力によるランタイムパラメータになったため、前処理側で特定の投影に固定する意味がなくなった。よって:

- **再投影(reproject)は行わない**。17タイルを緯度経度グリッドのまま**単純にモザイク(タイル座標に基づくピクセル敷き詰め)**し、その後ダウンサンプリングするだけでよい
- 実際のメートル単位の座標(東/北/上)への変換は、**UIで指定された原点をもとにフロント側(Rust/WASM)がランタイムに行う**(3節参照)
- これによりGDALの役目は「GeoTIFFの読み取り・モザイク・ダウンサンプリング・書き出し」に限定され、再投影(warp)処理が不要になり前処理ツールがシンプルになる

### 2.3 モザイク

- 17タイルを緯度経度グリッド上のピクセル位置(タイルIDから一意に決まる)にそのまま敷き詰める。外接矩形: 北緯35°〜40°・東経135°〜140° → 18000×18000px(3600px/度 × 5度)
- 欠損8タイル分(1.4節)は標高0mで埋める

### 2.4 ダウンサンプリング

- 目標解像度: **1024×1024**(ユーザー確定)
- リサンプリング手法: **平均法(average)**を第一候補とする(18000÷1024≈17.6倍の間引きになるため、最近傍やバイリニアだと標高の代表性が落ちる。平均法でノイズを均す)
- 緯度方向1セルあたり 5°÷1024 ≈ 0.00488°(≈542m)。経度方向も同程度(この緯度帯ではやや狭くなる)。元の30m解像度から見ると約18倍の間引きとなり、山岳の鋭いピークは丸まるが、広域俯瞰用途としては許容範囲

### 2.5 出力

| ファイル | 内容 | 備考 |
|---|---|---|
| `heightmap.bin` | f32, row-major, little-endian, 1024×1024, **標高値(海抜メートル、DSM値そのまま)** | 緯度経度グリッド上の値。ENU変換前の生データ |
| `texture.png` | **生成しない**(v1) | ユーザー確定事項。フロント側は標高グラデーション着色を使う |
| `metadata.json` | 下記2.6参照 | 緯度経度範囲・標高範囲を含む |

### 2.6 `metadata.json` 拡張仕様

指示書のスキーマ(`bounds`がメートル座標だった部分)を、**緯度経度ベース**に置き換える。実際のメートル座標(ENU)への変換はフロント側の責務とする。

```json
{
  "width": 1024,
  "height": 1024,
  "elevation_min": -102.0,
  "elevation_max": 3771.0,
  "geodetic_bounds": {
    "min_lat": 35.0,
    "max_lat": 40.0,
    "min_lon": 135.0,
    "max_lon": 140.0
  },
  "source_crs": "EPSG:4326 (WGS84相当, GRS80楕円体)",
  "height_datum": "orthometric height (EGM96 geoid) — ALOS DSM仕様に準拠。海抜=ジオイド高として扱う",
  "ellipsoid": { "a_m": 6378137.0, "inv_f": 298.257222101 },
  "has_texture": false,
  "default_origin": {
    "lat_dms": "35°21'20\"N",
    "lon_dms": "138°51'35\"E",
    "lat_deg": 35.355556,
    "lon_deg": 138.859722
  }
}
```

- `geodetic_bounds`: 各グリッドセル(i,j)の緯度経度は `lat = min_lat + (j / (height-1)) * (max_lat - min_lat)`、経度も同様に線形補間で求まる(等間隔の緯度経度グリッドのため)
- `elevation_min`/`elevation_max`は前処理時に実測して埋める(上記実測値はあくまで現時点の参考値)
- `has_texture`: フロント側が`texture.png`の有無を毎回fetch失敗で判定せずに済むよう明示フラグとして追加(指示書にはなかった項目)
- `default_origin`: UIの原点入力欄の初期値。実際に使われる原点はサーバー側が保持する状態が正(4節のOriginStateを参照)であり、これはあくまで未接続時のUI初期表示用
- `ellipsoid`/`height_datum`: フロント側のENU変換計算(3節)で使うパラメータ

---

## 3. 基準位置(原点)とENU座標系

### 3.1 定義

- シミュレーション座標系 = **東=X、北=Y、鉛直上向き=Z** の局所ENU(East-North-Up)座標系
- 原点 = UIで入力された緯度経度地点の、**海抜0m(ジオイド高0m)の点**
- デフォルト原点(試験用): **北緯35°21'20"、東経138°51'35"**(10進: 35.355556°, 138.859722°)。地形データの範囲内(タイル`N035E138`内)に収まっており、そのまま試験に使える

### 3.2 変換式(緯度経度+標高 → ENU)

地形グリッドの各点は`(lat, lon, h)`(hはDSMの標高値=海抜メートル)で表現される。原点`(lat0, lon0)`が決まれば、標準的な測地座標→ECEF→ENU変換で一意にメートル座標が求まる。GRS80楕円体(`metadata.json`の`ellipsoid`)を用いる。

```
a = 6378137.0                      // 長半径
f = 1 / 298.257222101              // 扁平率
e2 = f * (2 - f)                   // 離心率の2乗

// 1) 測地座標 → ECEF
N(lat) = a / sqrt(1 - e2 * sin(lat)^2)
X = (N(lat) + h) * cos(lat) * cos(lon)
Y = (N(lat) + h) * cos(lat) * sin(lon)
Z = (N(lat) * (1 - e2) + h) * sin(lat)

// 2) 原点(lat0, lon0, h0=0)のECEFを基準に差分を取り、原点でのローカル座標系に回転(ENU)
dX = X - X0,  dY = Y - Y0,  dZ = Z - Z0

East  = -sin(lon0)*dX + cos(lon0)*dY
North = -sin(lat0)*cos(lon0)*dX - sin(lat0)*sin(lon0)*dY + cos(lat0)*dZ
Up    =  cos(lat0)*cos(lon0)*dX + cos(lat0)*sin(lon0)*dY + sin(lat0)*dZ

// シミュレーション座標
sim_x = East
sim_y = North
sim_z = Up
```

- 高さの扱い: DSM値はEGM96ジオイド基準の海抜(orthometric height)、上式の`h`にはそのままこの値を使う(ジオイド高とだ円体高の差=ジオイド高低差(日本国内でおおむね30〜40m、変動幅は対象範囲内で数m程度)は本設計では無視する簡略化とする。地形の格子間隔がすでに約540mあることを踏まえると実用上の影響は小さい)
- Earthの曲率は上式に自然に反映される(遠方の地点ほど`Up`が減少していく)。550km規模の広域では原点から離れるほど地表が「下に沈んで」見える効果が正しく表現される

### 3.3 計算をどこで行うか

- **前処理(C++/GDAL)側では行わない**。`heightmap.bin`/`metadata.json`は原点非依存の生データのまま
- **フロント側(Rust/WASM)がメッシュ構築時・原点変更時にランタイムで計算する**。1024×1024≈105万頂点分の変換は原点ごとに1回の行列(回転)計算+単純な座標変換で済み、WASM上でも軽量(originが変わるたびに数十ms程度を想定。頻繁に変わるものではないため許容範囲)
- 原点を変更したら、地形メッシュの頂点バッファを再計算・再アップロードする(`heightmap.bin`/`texture.png`の再フェッチは不要)

### 3.4 原点の変更タイミング

原点はシミュレーション座標系の定義そのものであり、シミュレーション実行中に変更するとC++側・フロント側双方の状態(位置、地形メッシュ)がずれるリスクがある。本書では以下を設計方針として採用する:

- **シミュレーション開始前(停止中)のみ原点変更可能**とし、実行中はUIの原点入力をロックする
- 原点はサーバー(C++側)が正とする状態であり、UIはサーバーから配信された値を表示・編集する(4節参照)。ユーザーの「シミュレーション本体もUIと同じ座標系とする」との確定方針に基づき、C++側は原点についてUIとは別の内部表現を持たず、サーバーが保持する原点state(=`OriginState`として配信される値)を唯一の真実とする

### 3.5 原点入力のバリデーション

指示書未確定事項への回答: 地形データ範囲外・極端な緯度経度は**そもそも入力・送信できないようにする**。

- UIの原点入力フォームは、`metadata.json`の`geodetic_bounds`(北緯35°〜40°・東経135°〜140°)の範囲を入力可能な値の上下限として使い、範囲外の値は**入力欄への入力段階でブロックする**か、送信ボタンを無効化して`set_origin`自体を送信できないようにする
- サーバー側でも同じ範囲チェックを行う(フロントのバリデーションを回避するクライアントに対する防御的チェック)。万一範囲外の`set_origin`が送られてきた場合は4.3節の`CommandError`を返す

---

## 4. 通信プロトコルへの追加

指示書の通信プロトコル設計(§通信プロトコル設計)に対し、以下を追加する。

### 4.1 新規サーバー→クライアントメッセージ: `OriginState`

```
msg_type: 0x03 = OriginState (原点情報、状態変化時のみ送信。接続直後にも1回送信)

lat_deg: f64
lon_deg: f64
```

- 接続確立時にサーバーが現在の原点(未設定なら`default_origin`)を送信する
- 全クライアントに同一の原点を配信する(マルチクライアントは全員同一ブロードキャストで確定。原点も同じ方針に合わせる)

### 4.2 `ClientCommand`の拡張: `set_origin`

指示書の`ClientCommand`(`type`/`button_id`/`value`)に、原点設定用のフィールドを追加する。

```
type: String            ("set_origin" を追加)
button_id: String       (vab_press時のみ使用、既存)
value: f64              (set_param時のみ使用、既存)
lat_deg: f64            (set_origin時のみ使用、新規)
lon_deg: f64            (set_origin時のみ使用、新規)
```

- UIの原点入力フォームから送信し、サーバー(C++側)が(a)シミュレーションが停止中であることを確認、(b)`geodetic_bounds`範囲内であることを確認、(c)状態を更新、(d)全クライアントに`OriginState`を再送信、という流れにする
- (a)(b)いずれかを満たさない場合は状態を更新せず、要求元クライアントに4.3節の`CommandError`を返す

### 4.3 新規サーバー→クライアントメッセージ: `CommandError`

指示書未確定事項への回答: `set_origin`が拒否された場合、サーバーは**黙って無視せずエラーを返す**。汎用的なコマンド拒否応答として設計する(将来他のコマンド種別にも流用できる)。

```
msg_type: 0x05 = CommandError (コマンド拒否応答。要求元クライアントのみに送信)

command_type: String   // 拒否されたClientCommandのtype ("set_origin" 等)
message: String        // エラー内容(人間可読な説明文)
```

- `set_origin`が拒否されるケース: (1) シミュレーション実行中(3.4節)、(2) 緯度経度が地形データの`geodetic_bounds`範囲外(3.5節)
- ブロードキャストはせず、要求を送ったクライアントにのみ返す

### 4.4 新規サーバー→クライアントメッセージ: `StatusPanelConfig`

右パネル上部「各種情報」の表示項目を**C++側から動的に設定できる**ようにする(指示書未確定事項5への回答)。`VabConfig`と同じ「低頻度の構成メッセージ + 高頻度の値配信」のパターンを踏襲する。

```
msg_type: 0x04 = StatusPanelConfig (状況パネル項目定義、状態変化時のみ送信。接続直後にも1回送信)

items: Vec<StatusItem>
  StatusItem:
    id: String
    label: String
    unit: String        // 単位。なければ空文字列
```

実際の値は、指示書の`SimState`(msg_type 0x01, 高頻度)に新しいフィールドを追加して配信する。

```
SimState (拡張後):
  t: f64
  positions: Vec<f32>
  frame_id: u32
  status_values: Vec<f64>   // StatusPanelConfig.items と同じ順序・同じ数
```

- 表示項目の内容(例: 高度、速度、経過時間、残燃料 等)はC++側が`StatusPanelConfig`で自由に定義し、フロント側は項目をハードコードせず`items`の定義通りに`status_values`を並べて表示する
- v1では**数値項目のみ**を対象とする(テキスト値が必要になった場合は別途拡張。本書スコープ外)

### 4.5 WebSocket再接続処理

指示書未確定事項への回答: フロント側は切断を検知したら**自動的に再接続する**。「無制限リトライによるリソース消費への配慮」も合わせて確定した(以下の3点)。

- 再接続間隔は**指数バックオフ**(初回1秒、以後2倍ずつ、上限30秒でキャップ)とする
- バックオフ間隔に**ジッター(±数百ms程度のランダムな揺らぎ)**を加える。サーバー再起動時に多数のクライアントが同時に再接続を試みる「サンダリングハード」を避けるための配慮
- **ブラウザタブが非表示(バックグラウンド)の間は再接続の試行を一時停止**する(Page Visibility APIで検知)。タブがアクティブに戻ったタイミングで再接続を再開する。無駄なリトライによるリソース消費を抑えるための配慮
- リトライ回数そのものの上限は設けず、**接続できるまで(タブが表示されている間は)無制限にリトライし続ける**
- 再接続中であることをUIに表示する(状況パネル付近に簡易な接続ステータス表示を設ける。具体的な表示方法は実装時に決定)
- 再接続成功後は、サーバーから`OriginState`/`VabConfig`/`StatusPanelConfig`が接続直後の仕様により再送されるため、フロント側の表示状態は自然に復旧する

---

## 5. 地形描画(Rust/wgpu側)の変更点

指示書 §「Rust(Leptos/WASM)側 実装要件」からの差分。

### 5.1 頂点構造の変更

テクスチャを使わないため、UV座標の代わりに標高由来の色を持たせる。また座標軸の意味を指示書のY-up想定から、本書で確定した**Z-up(東=X, 北=Y, 上=Z)**のシミュレーション座標系に合わせる。

```rust
#[repr(C)]
#[derive(Clone, Copy, bytemuck::Pod, bytemuck::Zeroable)]
struct TerrainVertex {
    position: [f32; 3], // x(East), y(North), z(Up) — 3節のENU変換結果
    color: [f32; 3],    // 標高グラデーション色(CPU側で事前計算)
}
```

- 頂点位置は「ロード時 + 原点変更時」に3.2節の変換式で都度計算する(`metadata.json`の`geodetic_bounds`からグリッド各点の`lat`/`lon`を求め、DSM値`h`と合わせてENU変換)
- wgpu/カメラ側は一般的なY-up前提のライブラリ慣習と食い違うため、`camera.rs`のビュー行列構築時に軸の対応を明示的に扱う(例: レンダリング内部ではsim_zをY-upのYに割り当てる、等)。実装時に混同しないようコード上のコメントで axis 対応を明記すること
- `texture.png`関連のロード処理(`createImageBitmap`等)は`metadata.json`の`has_texture`が`true`の場合のみ実行する分岐にしておく(v1では常に`false`なので事実上未使用コードパスだが、将来オルソ画像が手に入った場合への拡張余地は残す)
- グラデーション配色: 標高を`elevation_min`〜`elevation_max`で正規化し、低地(緑〜青みの低彩度)→高山(白に近い明色)のような地形図的カラーランプを既定とする(具体的なカラーストップは実装時に調整可能なテーブルとして持つ)

### 5.2 カメラ(自由視点)

指示書の未確定事項12への回答: 固定カメラのMVPは採用せず、**最初から自由に操作できるカメラ**とする。

- **ズーム(拡大縮小)**: マウスホイール / タッチのピンチ操作で、注視点への距離を自由に変更できる
- **角度切り替え(回転)**: ドラッグ操作で、注視点を中心とした水平・垂直方向の回転(オービットカメラ)ができる
- **視点切り替え**: 「俯瞰」「側面」等のプリセット視点をワンクリックで呼び出せるビュー切り替えUIを用意する。プリセットはあくまで初期アングルの提供であり、切り替え後もズーム・回転の自由操作は継続できる
- 中央パネル(地図)・右パネル下部(側面図)は指示書の方針通り**同一の地形メッシュに異なるカメラ(ビュー行列)を適用する**構成のまま、双方とも上記の自由操作に対応させる
- カメラの状態(位置・角度・ズーム)はLeptosの`Signal`で保持し、パネルごとに独立させる

---

## 6. UIレイアウト・VAB・状況パネルの確定

指示書のUIレイアウト・VAB仕様(§UIレイアウト)に対する、未確定事項の確定内容。

### 6.1 パネルレイアウトのレスポンシブ・リサイズ対応

- **左パネル**: 指示書通り**固定幅**(`--panel-width`)のまま。レスポンシブ対応は行わない
- **中央(地図)・右パネル**: **レスポンシブ対応**させる。また、**ユーザーがUI上でサイズを変更できる**ようにする
  - 地図と右パネルの境界に、ドラッグ可能なリサイザー(splitter)を設置する
  - CSS Gridの`grid-template-columns`のうち地図・右パネルに対応する幅をLeptosの`Signal`で保持し、ポインタドラッグに応じてリアルタイムに反映する
  - リサイズ結果の永続化(localStorage等)は本設計のスコープ外とし、**セッション内(ページ再読み込みまで)のみ保持**する
- **レスポンシブの方式**: 指示書未確定事項への回答として、**3カラムの横並びレイアウトは崩さない**(狭幅時に右パネルが地図の下へ積まれる、といった縦積みへの再レイアウトは行わない)
  - 画面幅が狭くなった場合は、各カラム(特に中央・右パネル)の幅を比例的に縮小するだけにとどめる
  - それでも内容が収まらない最小幅を下回った場合は、3カラムを内包する外側コンテナに`overflow-x: auto`を設定し、**横スクロールで対応する**(改行・縦積みは行わない)
  - 各パネル内のコンテンツ(VABグリッド、状況パネルの項目リスト等)が縦方向に収まらない場合は、パネル単位で縦スクロール(`overflow-y: auto`)を設ける

### 6.2 VAB仕様の確定

- 開発用ダミー`VabConfig`の初期値: **rows=4, cols=6**(24ボタン)
- ボタンの操作種別: **単純クリックのみ**(長押し・トグル等は実装しない)
- ラベルが空文字の`VabButton`は「未使用の穴」として、**ボタン要素自体を描画しない**(空の`<button>`や無効化ボタンではなく、DOM上に何も生成しない)。グリッド上のその位置は空白のまま残し、他のボタンの行・列位置がずれないようにする

### 6.3 状況パネルの確定

- 右パネル上部「各種情報」の表示項目(ラベル・単位・並び順)は、4.4節で追加した`StatusPanelConfig`により**C++側が動的に決定**する。フロント側は表示項目をハードコードしない
- 垂直誇張スライダー等、地形描画パラメータに関する表示項目は不要(0節の表・項目10参照、垂直誇張機能自体を実装しないため)

---

## 7. C++側 前処理ツールの変更点

指示書 §「GeoTIFF前処理ツール」処理ステップに対する具体化(再投影が不要になったことで大幅に簡略化):

1. 入力GeoTIFF群(17枚固定、`map_data/*_DSM.tif`)を読み込み(GDAL、デコード用途のみ。再投影・Warpは使わない)
2. 17タイルを緯度経度グリッド上のピクセル位置にそのまま敷き詰めてモザイク(18000×18000px)。実在しない8タイル分は標高0mで埋める
3. 平均法(average)で1024×1024にダウンサンプリング
4. `heightmap.bin`(f32、標高値そのまま)出力
5. `texture.png`は**生成しない**(ステップ自体スキップ)
6. `elevation_min`/`elevation_max`を実測し、2.6節のスキーマで`metadata.json`出力(`geodetic_bounds`/`ellipsoid`/`default_origin`を含む)

C++シミュレーション本体側には、4節で追加した`OriginState`/`set_origin`/`StatusPanelConfig`/`status_values`の実装(現在の原点・状況パネル項目を保持する状態変数、原点はシミュレーション停止中のみ変更を許可するガード)が新たに必要になる。

---

## 8. 未確定事項の解消状況

指示書の未確定事項12項目(0節の表)に加え、その確定に伴って新たに生じた5つの実装レベルの未決事項も、すべて本書で方針を確定した。

| # | 項目 | 確定内容 |
|---|---|---|
| 1 | `set_origin`のエラー応答形式 | サーバーは**黙って無視せずエラーを返す**。新規`CommandError`メッセージを追加(4.3節) |
| 2 | 緯度経度入力のバリデーション | 地形データ範囲外の値は**そもそも入力・送信できないようにする**(UI側でブロック、サーバー側でも防御的チェック。3.5節) |
| 3 | レスポンシブの具体的な方式 | **縦積みへの再レイアウトは行わない**。狭幅時は幅を比例的に縮小するのみとし、収まらない場合は横スクロールで対応する(6.1節) |
| 4 | VABの空ラベルボタンの扱い | **ボタン要素自体を描画しない**(空欄のまま、DOM上に生成しない。6.2節) |
| 5 | 無制限WebSocket再接続のリソース消費 | **配慮する**: バックオフにジッターを加え、タブが非表示の間は再接続を一時停止する(4.5節) |

本書時点で、指示書由来の未確定事項および本書で新たに生じた実装レベルの未決事項に、未回答のものは残っていない。今後の実装過程で新たに生じる細部の判断(エラーメッセージの具体的な文言、CSS変数の具体的な数値など)は、実装者が本書の方針に沿って妥当な値を定めてよい。

---

## 9. 実装フェーズ(指示書からの更新)

指示書の「実装フェーズ(推奨順序)」を踏襲しつつ、地形パイプライン・原点・状況パネル・カメラまわりを本書の確定事項に合わせて読み替える。

1. C++側: `protocol.hpp`定義(`OriginState`/`set_origin`/`StatusPanelConfig`/`status_values`拡張含む) + uWebSocketsサーバーの雛形(ダミーデータ送信のみ)
2. Rust側: WebSocket接続 + 受信データのデシリアライズ確認(`OriginState`/`StatusPanelConfig`/`CommandError`受信を含む)+ **自動再接続(指数バックオフ+ジッター、タブ非表示時は一時停止)**の実装
3. Rust側: 3カラムレイアウトの骨組み(左パネル固定、**地図・右パネルはリサイザー付き**で配置)
4. Rust側: VABコンポーネント実装(**rows=4, cols=6**のダミー`VabConfig`、**単純クリックのみ**)
5. C++側: 実シミュレーションループとの結合、コマンドキュー実装、原点状態の保持・変更ガード実装
6. C++側: GeoTIFF前処理ツール最小実装(**1タイルのみ**で`heightmap.bin`/`metadata.json`出力を確認。再投影なしのシンプルな変換であることを検証)
7. C++側: **17タイル全体のモザイク**(欠損部0m埋め含む)+ 1024×1024ダウンサンプリング対応
8. Rust側: `heightmap.bin`取得 → `geodetic_bounds`からlat/lon復元 → **ENU変換(原点=デフォルト値)**→ メッシュ生成 →**標高グラデーション着色**で描画確認(テクスチャ処理は実装しない)
9. Rust側: **原点入力フォーム**(`geodetic_bounds`範囲外を入力させないバリデーション、`set_origin`送信・`OriginState`反映・`CommandError`表示・メッシュ再計算)、**状況パネル**(`StatusPanelConfig`駆動)の実装
10. Rust側: **自由視点カメラ**(ズーム・回転・視点切り替え、俯瞰・側面の2パネル双方)の実装
11. UI細部の仕上げ: VABの空ラベルボタン非表示、パネルの横スクロール対応、再接続ステータス表示

---

## 10. ディレクトリ構成(更新なし)

C++側・Rust側ともに指示書記載のディレクトリ構成をそのまま採用する(差分なし)。地形入力元のみ以下を追記:

```
map_data/                    # ALOS全球数値地表モデル(入力、リポジトリ内に配置済み)
├── ALPSMLC30_N035E135_DSM.tif
├── ...(17タイル×6ファイル)
```
